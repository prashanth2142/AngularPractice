import { TopscrollbarDirective } from './topscrollbar.directive';

describe('TopscrollbarDirective', () => {
  it('should create an instance', () => {
    const directive = new TopscrollbarDirective();
    expect(directive).toBeTruthy();
  });
});
